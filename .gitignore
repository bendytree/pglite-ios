# build outputs
build/
.build/
native/gen/
DerivedData/
*.xcresult

# module build state (sha-pinned fetches + docker work tree + artifacts;
# artifacts ship as GitHub release assets, not in git)
module/cache/
module/work/
module/out/

# generated Xcode project (xcodegen generate)
examples/DeviceHost/PGliteHost.xcodeproj/

# phase 0–2 working directories (research provenance, kept locally;
# reports live in docs/)
phase0/
phase1/

# logs / scratch
*.log
.DS_Store
